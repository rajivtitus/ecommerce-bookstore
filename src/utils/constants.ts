export enum Paths {
	Home = "/",
	Cart = "/cart",
	Products = "/products",
	ProductDetails = "products/:id",
	Other = "*",
}

export enum NavItems {
	Home = "Home",
	Products = "Products",
	Cart = "Cart",
}
